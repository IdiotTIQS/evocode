# 复制为 .env 并填入真实值后运行 docker compose up。
# JWT 签名密钥：必须 >=32 字节。生成示例：
#   openssl rand -hex 32
# 下面是占位值（故意过短）：直接照搬会让控制平面在 JWT 初始化时报错退出，
# 提醒你换成真实随机密钥——而非用一个提交在源码里的“看似有效”的密钥静默启动。
EVOCODE_JWT_SECRET=REPLACE_ME
